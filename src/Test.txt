taskkill /IM msedge.exe /F



$profile = Join-Path $env:TEMP ("edge-cdp-" + (Get-Date -Format "yyyyMMdd-HHmmss")); `
Start-Process "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" "--remote-debugging-port=9222 --user-data-dir=$profile"; `
Start-Sleep -Seconds 2; `
$CDP = (Invoke-RestMethod http://localhost:9222/json/version).webSocketDebuggerUrl;



npm install -g allure-commandline
allure --version


npm install -D allure-playwright
npm list allure-playwright

New-Item -ItemType Directory -Force reports\allure-results | Out-Null
New-Item -ItemType Directory -Force reports\allure-report | Out-Null
New-Item -ItemType Directory -Force reports\html | Out-Null
New-Item -ItemType Directory -Force reports\json | Out-Null
New-Item -ItemType Directory -Force reports\junit | Out-Null

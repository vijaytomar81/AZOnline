Error 1) 
Getting error in all business.ts files (beliw is from AuthEntryPage.ts)
import { elements, type ElementKey } from "./elements";

it should be like below:

import { elements } from "./elements";
import type { ElementKey } from "./elements";

Error 2) 
Getting error in below in alises files:

// Start empty. You can map business names -> ElementKey.
export const aliases = {
  // example: continue: aliasesGenerated.next,
} as const satisfies Record<string, ElementKey>;

error details:

const satisfies: any
',' expected.

const Record: any
',' expected.

'>' expected.

any
'ElementKey' only refers to a type, but is being used as a value here.
